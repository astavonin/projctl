platform: github

github:
  repo: "astavonin/projctl"

labels:
  default:
    - "enhancement"

common:
  issue_template:
    required_sections:
      - "Description"
      - "Acceptance Criteria"

# Planning sync configuration
planning_sync:
  gdrive_base: ~/GoogleDrive
